# Harbor mode — updates allowed on SLA-2/3 via Watchtower overlay
# Copy to harbor.env on each Pi (not committed)

RACE_MODE=false
WATCHTOWER_NO_PULL=false

# Optional: enable grib-ingest scheduled fetch
ONLINE_MODE=true

# Compose project name per host
COMPOSE_PROJECT_NAME=ai-sailing
